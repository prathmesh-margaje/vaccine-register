import { VaccineRegistration } from './vaccine-registration';

describe('VaccineRegistration', () => {
  it('should create an instance', () => {
    expect(new VaccineRegistration()).toBeTruthy();
  });
});
